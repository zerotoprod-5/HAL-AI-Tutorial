# Private, local-only session context — never publish
CLAUDE.md
CLAUDE_*.md
claude_backup.txt
.claude/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Node deps (not used at runtime — decks are dependency-free)
node_modules/

# Internal build & QA tooling — kept locally, not published to the viewer-facing repo.
# (Generators for the notebooks/figures/decks, and the deck interaction-test harness.)
build/
deck/audit.mjs
deck/audit.README.txt

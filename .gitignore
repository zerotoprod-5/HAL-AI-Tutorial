# Private, local-only session context — never publish
CLAUDE.md
CLAUDE_*.md
claude_backup.txt
.claude/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Node deps (not used at runtime — decks are dependency-free)
node_modules/

# Internal build & QA tooling — kept locally, not published to the viewer-facing repo.
# (Generators for the notebooks/figures/decks, and the deck interaction-test harness.)
build/
deck/audit.mjs
deck/audit.README.txt

# Presenter & committee materials — kept locally, not published to the attendee-facing repo.
# (Teaching script/answers and organising-committee paperwork.)
SPEAKER_NOTES.md
LESSON_PLAN.md
RUN_OF_SHOW.md
SESSION_PLAN.md
Predictive-AI-Lesson-Plan.docx
Predictive-AI-Lesson-Plan.pdf
Predictive-AI-Session-Plan.docx

# Session-3 runtime download/extraction at the repo/working root ONLY.
# The committed dataset lives at audio/fan_clips.zip and must stay tracked,
# so these patterns are anchored with a leading slash.
/fan_clips/
/fan_clips.zip
